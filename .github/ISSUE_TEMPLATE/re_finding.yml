name: Reverse-engineering finding
description: Capture a protocol or register-level finding with evidence, hypothesis, and falsification path.
labels: ["re-finding"]
body:
  - type: input
    id: protocol
    attributes:
      label: Protocol / surface
      placeholder: "eBUS B524, Portal, MCP, VRC Explorer, ..."
    validations:
      required: true

  - type: input
    id: topology
    attributes:
      label: Device / topology
      placeholder: "controller, adapter path, mux shape, relevant addresses"
    validations:
      required: true

  - type: textarea
    id: raw_evidence
    attributes:
      label: Raw evidence
      description: Registers, frames, dumps, snapshots, and exact observations.
      render: shell
    validations:
      required: true

  - type: textarea
    id: hypothesis
    attributes:
      label: Hypothesis
      description: State the current interpretation. If it is already proven, say why.
      placeholder: "Hypothesis: ..."
    validations:
      required: true

  - type: textarea
    id: falsification
    attributes:
      label: Falsification path
      description: Describe what evidence would disprove the hypothesis.
      placeholder: "This would be false if ..."
    validations:
      required: true

  - type: input
    id: docs_target
    attributes:
      label: Docs target
      description: Which corresponding protocol or semantic/platform docs repository and path owns this knowledge?
      placeholder: "Project-Helianthus/helianthus-docs-<protocol>: path/..."

  - type: input
    id: execution_guide
    attributes:
      label: Execution guide
      description: Link the merged human-readable guide if this finding belongs to a guide-backed workstream.
      placeholder: "https://github.com/Project-Helianthus/helianthus-execution-plans/..."

  - type: checkboxes
    id: licensing_ack
    attributes:
      label: Licensing acknowledgement
      description: Confirm the licensing model for any material you submit for inclusion in Helianthus.
      options:
        - label: I have read LICENSING.md and I accept the Helianthus licensing model for any contribution or reusable material I submit here.
          required: true
